
#ifndef __VALUE_SELECTION_H__
#define __VALUE_SELECTION_H__

#include <datastore/FieldDescriptor.h>
#include <vector>

namespace DataStore
{
  /*
  class Selection
  {
  public:
    static bool SelectFields(const IFieldDescriptors& fieldDescriptors, 
      const FieldValues& inFieldValues, FieldValues* outSelection);

    static bool SelectField(const IFieldDescriptor& fieldDescriptor, 
      const FieldValues& inFieldValues, FieldValue* outValue);
  };
  */
}

#endif
